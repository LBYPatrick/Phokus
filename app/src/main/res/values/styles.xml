<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->

    <style name="CameraTheme.Toggle" parent="">
        <item name="android:layout_width">60dp</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:elevation">4dp</item>
        <item name="android:layout_marginTop">10dp</item>
    </style>

    <!-- Deprecate soon -->
    <style name="CameraTheme.Toggle.Button" parent="CameraTheme.Toggle">
        <item name="android:outlineAmbientShadowColor" tools:ignore="NewApi">#00FFFFFF</item>
        <item name="android:outlineSpotShadowColor" tools:ignore="NewApi">#00FFFFFF</item>
        <item name="android:textSize">10dp</item>
        <item name="elevation">4dp</item>
        <item name="backgroundTint">@color/colorCameraButton_43</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>
</resources>